pipeline { 
     agent {label "docker_slave"}
    
    stage('Print Branch name') {
        steps {
                    
             echo "${env.GIT_BRANCH}"

           }
        }
    
        

}

